//
//  ContentView.swift
//  Shared
//
//  Created by Brennan Drew on 8/10/20.
//

import SwiftUI

/*
struct ContentView: View {
    //@Binding var selection: Node?
    
    var body: some View {
        
        NavigationView {
            VStack {
                List(/*selection: $selection*/) {
                    OutlineGroup(sampleData, children: \.children) { node in
                        Label("\(node.text)", systemImage: node.shape ?? "circle")
                    }
                }.listStyle(SidebarListStyle())
            }.navigationTitle("Nodes")
        }
    }
}


struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
*/
